package serie6;

public class Exercice02 {
    public static void main(String[] args) {
        //to calculate the max number of iteration to find the value in an array with
        // the binary search is log2(tab.length)
        //the answer of the exercise is log2(300000) = 18.2 (approx)
    }
}
